---
const items = [
  {
    title: "Resizing",
    line: "Free within 60 days, any length, no questions.",
  },
  {
    title: "Repairs",
    line: "Lifetime, on anything we made. You pay shipping one way.",
  },
  {
    title: "Care",
    line: "Store flat and dry. Take it off before the pool, not the shower.",
  },
];
---

<section id="care" class="section-y bg-base-200" aria-labelledby="care-heading">
  <div class="container-halden">
    <div class="section-header">
      <p class="text-eyebrow">After you buy</p>
      <h2 id="care-heading" class="text-headline">
        Free resizing, lifetime repairs.
      </h2>
    </div>
    <div class="section-content grid grid-cols-1 gap-32 md:grid-cols-3">
      {
        items.map((item) => (
          <article class="card-alt flex flex-col gap-16">
            <h3 class="text-display-20">{item.title}</h3>
            <p class="text-body max-w-none">{item.line}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
